-- Supabase custom roles
-- This file defines custom roles for the Supabase project
-- Based on Supabase documentation, we ensure basic role setup is complete

-- No custom roles are currently needed for this project
-- All required roles (anon, authenticated, service_role, etc.) are handled by Supabase
SELECT 1; -- Ensure file is not empty and has valid SQL 